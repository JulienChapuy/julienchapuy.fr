---
import AboutReact from "./About/About";
import { siteData } from "../data/copy";

interface Props {
    lang: "fr" | "en";
}

const { lang } = Astro.props;
const content = siteData[lang].about;
---

<AboutReact content={content} client:load />
