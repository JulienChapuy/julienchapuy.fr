---
import BlogReact from './Blog/Blog';
import { siteData } from '../data/copy';

interface Props {
  lang: 'fr' | 'en';
  limit?: number;
}

const { lang, limit } = Astro.props;
const content = siteData[lang].insights;
---

<BlogReact content={content} limit={limit} lang={lang} client:load />
