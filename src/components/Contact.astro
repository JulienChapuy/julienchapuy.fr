---
import ContactReact from './Contact/Contact';
import { siteData } from '../data/copy';

interface Props {
  lang: 'fr' | 'en';
}

const { lang } = Astro.props;
const content = siteData[lang].contact;
---

<ContactReact content={content} client:load />
