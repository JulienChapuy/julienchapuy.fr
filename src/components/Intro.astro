---
import IntroReact from './Intro/Intro';
import { siteData } from '../data/copy';

interface Props {
  lang: 'fr' | 'en';
}

const { lang } = Astro.props;
const content = siteData[lang].intro;
---

<IntroReact content={content} stack={content.stack} client:load />
