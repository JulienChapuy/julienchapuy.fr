---
import ServicesReact from "./Services/Services";
import { siteData } from "../data/copy";

interface Props {
    lang: "fr" | "en";
}

const { lang } = Astro.props;
const content = siteData[lang].services;
---

<ServicesReact content={content} client:load />
