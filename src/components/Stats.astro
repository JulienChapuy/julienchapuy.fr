---
import StatsReact from './Stats/Stats';
import { siteData } from '../data/copy';

interface Props {
  lang: 'fr' | 'en';
}

const { lang } = Astro.props;
const content = siteData[lang].stats;
---

<StatsReact content={content} client:load />
